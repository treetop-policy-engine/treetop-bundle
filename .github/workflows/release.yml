name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTDOCFLAGS: "-D warnings"

jobs:
  verify:
    name: Verify release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Install Rust
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
        with:
          components: clippy, rustfmt
      - name: Verify tag version and main ancestry
        shell: bash
        run: |
          version="$(cargo metadata --no-deps --format-version 1 \
            | jq -r '.packages[] | select(.name == "treetop-bundle") | .version')"
          if [[ "$GITHUB_REF_NAME" != "v${version}" ]]; then
            echo "Tag $GITHUB_REF_NAME does not match treetop-bundle ${version}." >&2
            exit 1
          fi
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Release commit is not on origin/main." >&2
            exit 1
          fi
      - name: Cache Cargo artifacts
        uses: Swatinem/rust-cache@49a0bdc70d2e1b713ca9e2869b211fcce03d3c1c # v2
      - name: Verify source
        run: |
          cargo fmt --all --check
          cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
          cargo test --locked --workspace --all-features
          cargo doc --locked --workspace --all-features --no-deps
          cargo publish --locked -p treetop-bundle --dry-run

  publish:
    name: Publish library
    needs: verify
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
      - name: Check whether the crate version exists
        id: published
        shell: bash
        run: |
          status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' \
            --retry 3 --retry-all-errors --connect-timeout 10 --max-time 30 \
            --header 'User-Agent: treetop-bundle release workflow' \
            "https://crates.io/api/v1/crates/treetop-bundle/${GITHUB_REF_NAME#v}")"
          case "$status" in
            200) echo "published=true" >> "$GITHUB_OUTPUT" ;;
            404) echo "published=false" >> "$GITHUB_OUTPUT" ;;
            *) echo "Unexpected crates.io response $status." >&2; exit 1 ;;
          esac
      - name: Authenticate to crates.io
        if: steps.published.outputs.published != 'true'
        id: auth
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
      - name: Publish treetop-bundle
        if: steps.published.outputs.published != 'true'
        run: cargo publish --locked -p treetop-bundle
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  binaries:
    name: Publish CLI archives
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
      - name: Install cross
        run: cargo install cross --version 0.2.5 --locked
      - name: Build Linux musl binaries
        run: |
          for target in x86_64-unknown-linux-musl aarch64-unknown-linux-musl; do
            CARGO_TARGET_DIR="target/cross-${target}" \
              cross build --locked --release --target "$target" -p treetop-bundle-cli
          done
      - name: Package and smoke-test archives
        run: |
          mkdir release-artifacts
          target/cross-x86_64-unknown-linux-musl/x86_64-unknown-linux-musl/release/treetop-bundle \
            --version
          for architecture in x86_64 aarch64; do
            target="${architecture}-unknown-linux-musl"
            binary="target/cross-${target}/${target}/release/treetop-bundle"
            archive="release-artifacts/treetop-bundle-${architecture}-linux-musl.tar.gz"
            tar czf "$archive" -C "$(dirname "$binary")" treetop-bundle
          done
          sha256sum release-artifacts/*.tar.gz > release-artifacts/SHA256SUMS
      - name: Create GitHub release
        uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd # v1.21.0
        with:
          tag: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          generateReleaseNotes: true
          artifacts: "release-artifacts/*"
